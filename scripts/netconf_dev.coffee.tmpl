#
# Marketplace Client's network configuration (dev env)
#

module.exports = netConf = (s) ->
  s
    .replace /marketplace_uri/, "127.0.0.1"
    .replace /marketplace_port/, "43443"
    .replace /rendezvous_uri/, "/dns4/localhost/tcp/61617/ws/p2p-websocket-star"
